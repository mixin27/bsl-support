import 'package:flutter/material.dart';

class FootballLiveView extends StatelessWidget {
  const FootballLiveView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Football Live API"),
    );
  }
}
