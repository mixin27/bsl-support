import 'package:flutter/material.dart';

class LiveSportView extends StatelessWidget {
  const LiveSportView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Live Sport API"),
    );
  }
}
